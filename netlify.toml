[build]
  command = "npm run build"

[build.environment]
  # Next 16 exige Node >= 20.9 ; on force 22 (la valeur par défaut de Netlify
  # peut être plus ancienne selon l'âge du site).
  NODE_VERSION = "22"

[functions."weekly-stats"]
  schedule = "0 9 * * 1"


